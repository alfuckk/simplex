apiVersion: v1
kind: Service
metadata:
  name: {{ include "geoip-app.fullname" . }}
  labels:
    {{- include "geoip-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geoip-app.selectorLabels" . | nindent 4 }}
